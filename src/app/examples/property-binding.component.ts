import { Component } from '@angular/core';

@Component({
    selector:'app-property-binding',
    template:`
        this is a multi line property binding component
    `
})
export class PropertyBindingComponent {

}